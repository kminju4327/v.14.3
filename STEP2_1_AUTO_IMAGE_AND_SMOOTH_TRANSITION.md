# STEP 2.1 — 전체 이미지 자동 생성 + 최종 화면 전환 개선

- 최종 상세페이지 생성 시작 시 전용 로딩 화면 표시
- 이전/입력/템플릿 화면이 잠깐 다시 노출되는 깜빡임 방지
- 상세페이지 콘텐츠 생성 완료 후 이미지가 없는 모든 섹션 자동 생성
- 전체 이미지 생성 진행률 표시
- 기존 이미지가 있는 섹션은 유지하고 누락된 섹션만 생성
- 섹션별 이미지 재생성/업로드 기능은 수정용으로 유지
- 생성 실패 시 기존 섹션의 failed 상태 유지
- 불필요한 setTimeout 제거
- npm run build 성공
